#include "main.h"

/**
 * print_numbers - prints the numbers, from 0 to 9, followed by a new lin
 * Return: void
 */

void print_numbers(void)
{
	for (char a = '0'; a < '10'; a++)
		_putchar(a);

	_putchar('\n');
}

